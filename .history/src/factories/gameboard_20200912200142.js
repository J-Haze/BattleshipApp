import shipFactory from "./ship"

const gameboardFactory = () => {
    let board = [];
    for (let i = 1; i <= 100; i++) {
        board.push(null)
    }
    console.log({ board })

    let placeShip = (shipLength, position) = >{
        for (i in board) {
            if 
        }
    }
}

gameboardFactory();


export default gameboardFactory;